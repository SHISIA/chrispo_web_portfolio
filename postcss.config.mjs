/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {}, // Tailwind CSS for utility-first styling
    autoprefixer: {}, // Autoprefixer to ensure cross-browser compatibility
  },
};

export default config;
